[master]
%{ for ip in master_ips ~}
${ip} ansible_user=${ansible_user} ansible_ssh_private_key_file=${ansible_ssh_private_key_file}
%{ endfor ~}

[workers]
%{ for ip in worker_ips ~}
${ip} ansible_user=${ansible_user} ansible_ssh_private_key_file=${ansible_ssh_private_key_file}
%{ endfor ~}
